@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Most popular events of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (Event e in Model.PreferredEvents)
    {
        @Html.Partial("EventsSummary", e)
    }
</div>